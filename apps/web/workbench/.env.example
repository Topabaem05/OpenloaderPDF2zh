# Public origin of the stateful FastAPI backend. Do not include a trailing slash.
# Leave blank only when the frontend is served by the same FastAPI process.
VITE_API_BASE_URL=https://your-openpdf2zh-backend.example.com
